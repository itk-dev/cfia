<section class="blogs blog-grid-3 z-sec">
    <div class="container">
        <div class="blog-5">
            <div class="row">
                {% for row in rows %}
                    {{row.content}}
                {% endfor %}
            </div>
        </div>
    </div>
</section> 