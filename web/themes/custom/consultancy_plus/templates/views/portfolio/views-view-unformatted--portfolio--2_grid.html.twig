<section class="portfolio portfolio-grid-2">
    <div class="container">
        <div class="row row-mr filter-item filtering">
            {{fields.field_category.content}}
            {% for row in rows %}
                {{row.content}}
            {% endfor %}
        </div>
    </div>
</section>            