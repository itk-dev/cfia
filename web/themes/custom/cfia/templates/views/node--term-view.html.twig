{% include directory ~ '/templates/component/card-item.html.twig' with
  {
    'ImageUri': file_url(content.field_image['#items'].entity.uri.value),
    'ImageAlt': content.field_image['#items'].alt,
    'Url': url('entity.node.canonical', { 'node': node.id() }),
    'Category': content.field_category.0,
    'Label': node.label,
  }
%}
